// import * as mongoose from 'mongoose';
// import { Document, Model } from 'mongoose';

// export class CarDao<T extends Document, K extends { _id?: string }> {
//   protected model: Model<T>;

//   protected constructor(model: Model<T>) {
//     this.model = model;
//   }

//   async
// }
